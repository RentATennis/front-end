import React from "react";

const InputSelect = () => {
  return (
    <div>
      <select name="marca" id="marca">
        {/* options que vem da api (Brands) */}
      </select>
    </div>
  );
};

export default InputSelect;
